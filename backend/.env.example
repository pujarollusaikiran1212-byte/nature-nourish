# MongoDB Connection String
# For Railway deployment, use MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?options

# Server Port ( Railway will set this automatically )
PORT=3000
