# Railway Environment Variables

# MongoDB Connection String (REQUIRED)
# Get this from MongoDB Atlas - click "Connect" → "Connect your application"
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/databasename?retryWrites=true&w=majority

# Server Port (Railway will set this automatically)
PORT=
